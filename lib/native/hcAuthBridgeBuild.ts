/**
 * Bump when changing Health Connect native auth handoff so APK/WebView can be verified against logcat/UI.
 * Shown on the Android wearables Health Connect UI (small footer).
 */
export const HC_AUTH_BRIDGE_BUILD_ID = '2026-05-02-token-bridge-v3' as const
