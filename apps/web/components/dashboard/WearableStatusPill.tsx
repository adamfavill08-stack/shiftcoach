"use client";

/**
 * Wearable status under body clock — disabled for now (dashboard test / cleaner home).
 * Restore previous implementation from git history when you want the pill back.
 */
export function WearableStatusPill() {
  return null;
}
